<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springdemo.mapper.BizWorkOrderMapper">

    <resultMap id="BizWorkOrderResultMap" type="com.example.springdemo.model.BizWorkOrder">
        <id column="order_id" property="orderId"/>
        <result column="order_code" property="orderCode"/>
        <result column="category_id" property="categoryId"/>
        <result column="accept_time" property="acceptTime"/>
        <result column="accept_user" property="acceptUser"/>
        <result column="user_phone" property="userPhone"/>
        <result column="user_region" property="userRegion"/>
        <result column="user_address" property="userAddress"/>
        <result column="order_content" property="orderContent"/>
        <result column="ai_candidate_category" property="aiCandidateCategory"/>
        <result column="ai_match_evidence" property="aiMatchEvidence"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" parameterType="Long" resultMap="BizWorkOrderResultMap">
        SELECT order_id, order_code, category_id, accept_time, accept_user,
               user_phone, user_region, user_address, order_content,
               ai_candidate_category, ai_match_evidence, status,
               create_time, update_time
        FROM biz_work_order
        WHERE order_id = #{orderId}
    </select>

    <select id="selectUnclassified" resultMap="BizWorkOrderResultMap">
        SELECT order_id, order_code, category_id, accept_time, accept_user,
               user_phone, user_region, user_address, order_content,
               ai_candidate_category, ai_match_evidence, status,
               create_time, update_time
        FROM biz_work_order
        WHERE category_id IS NULL
           OR category_id = 0
        ORDER BY create_time ASC
    </select>

    <update id="updateCategory" parameterType="com.example.springdemo.dto.ClassificationResult">
        UPDATE biz_work_order
        SET category_id = #{categoryId},
            ai_candidate_category = #{categoryName},
            ai_match_evidence = #{matchEvidence},
            update_time = NOW()
        WHERE order_id = #{orderId}
    </update>

    <insert id="insert" parameterType="com.example.springdemo.model.BizWorkOrder" useGeneratedKeys="true" keyProperty="orderId">
        INSERT INTO biz_work_order (order_code, category_id, accept_time, accept_user,
                                   user_phone, user_region, user_address, order_content,
                                   ai_candidate_category, ai_match_evidence, status,
                                   create_time, update_time)
        VALUES (#{orderCode}, #{categoryId}, #{acceptTime}, #{acceptUser},
                #{userPhone}, #{userRegion}, #{userAddress}, #{orderContent},
                #{aiCandidateCategory}, #{aiMatchEvidence}, #{status},
                NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.example.springdemo.model.BizWorkOrder">
        UPDATE biz_work_order
        SET order_code = #{orderCode},
            category_id = #{categoryId},
            accept_time = #{acceptTime},
            accept_user = #{acceptUser},
            user_phone = #{userPhone},
            user_region = #{userRegion},
            user_address = #{userAddress},
            order_content = #{orderContent},
            ai_candidate_category = #{aiCandidateCategory},
            ai_match_evidence = #{aiMatchEvidence},
            status = #{status},
            update_time = NOW()
        WHERE order_id = #{orderId}
    </update>

    <select id="countTotal" resultType="int">
        SELECT COUNT(*) FROM biz_work_order
    </select>

    <select id="countClassified" resultType="int">
        SELECT COUNT(*) FROM biz_work_order
        WHERE category_id IS NOT NULL AND category_id > 0
    </select>

    <update id="updateById" parameterType="com.example.springdemo.model.BizWorkOrder">
        UPDATE biz_work_order
        SET category_id = #{categoryId},
            ai_candidate_category = #{aiCandidateCategory},
            status = #{status},
            update_time = NOW()
        WHERE order_id = #{orderId}
    </update>
</mapper>
